{
  "name": "escoladanuvem-atividade-final",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "./node_modules/.bin/parcel build src/index.html --dist-dir dist --public-url '.'"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.7.0"
  },
  "dependencies": {
    "parcel": "^2.7.0"
  }
}
